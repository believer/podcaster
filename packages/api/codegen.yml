overwrite: true
schema: "http://localhost:4000/graphql"
documents: null
generates:
  lib/__generated__/graphql.d.ts:
    config:
      contextType: '../types#PodcastContext'
    plugins:
      - "typescript"
      - "typescript-resolvers"

